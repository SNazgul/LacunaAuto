# Add EF Entity and Migration

Create a new Entity Framework Core entity and prepare migration for PostgreSQL.

**Entity description:** [USER DESCRIPTION]

Requirements:
- Use latest EF Core 10 features
- Proper configuration in DbContext (OnModelCreating)
- Value objects, owned entities, JSON columns where appropriate
- Indexes, constraints, default values
- Migration name suggestion
- Seeding data if needed